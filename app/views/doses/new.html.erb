<div class="container">
  <div class="row">
    <div class="col-sm-6 offset-md-3">
      <h1 class="mt-4">Create a new dose</h1>
     <%= simple_form_for [@cocktail, @dose] do |f| %>
     <%= f.association :ingredient %>
     <%= f.input :description %>
     <%= f.button :submit, class: "btn btn-primary btn-block"%>
     <% end %>

   </div>
 </div>
</div>

